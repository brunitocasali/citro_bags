/**
 * Configuración del Meta Pixel (lado navegador).
 * El Pixel ID es público (aparece en el HTML). El Access Token de Conversions API
 * es SECRETO y vive solo en el backend (`server/.env` → META_CAPI_TOKEN).
 */
export const META_PIXEL_ID = '1670887204183026';

/** Moneda usada en los eventos de valor. */
export const META_CURRENCY = 'ARS';
